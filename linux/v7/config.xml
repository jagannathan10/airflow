<config>

    <!-- Token for Airflow → Agent communication -->
    <token>scb-airflowagent-37dfad198aeff7821938aac9</token>

    <!-- Only allow these IPs or CIDR ranges -->
    <allowed_ips>
        <ip>10.1.0.0/16</ip>
        <ip>10.193.106.181</ip>
    </allowed_ips>

    <!-- Block dangerous commands -->
    <command_blacklist>
        <cmd>shutdown</cmd>
        <cmd>reboot</cmd>
        <cmd>rm -rf /</cmd>
        <cmd>mkfs</cmd>
        <cmd>poweroff</cmd>
        <cmd>halt</cmd>
        <cmd>:(){ :|:& };:</cmd> <!-- fork bomb -->
    </command_blacklist>

    <!-- Simple rate limiting -->
    <rate_limit>
        <window_seconds>60</window_seconds>
        <max_requests>120</max_requests>
    </rate_limit>

</config>
